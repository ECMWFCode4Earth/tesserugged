downscaling_factor: 5  # increase in image size in each dimension
steps: [5,]  # list of integers that multiply to the downscaling factor; the generator will use UpSampling2D layers of these sizes, alternating with residual blocks.